<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgriClime"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity.PlantActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activity.CuacaActivity"
            android:exported="false"/>
        <activity
            android:name=".ui.activity.PeringatanActivity"
            android:exported="false"/>
        <activity
            android:name=".ui.activity.SaranActivity"
            android:exported="false"/>
        <activity
            android:name=".ui.activity.UdaraActivity"
            android:exported="false"/>
        <activity
            android:name=".ui.activity.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true" />
    </application>

</manifest>